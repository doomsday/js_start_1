'use strict'

// // Generator returns strings of text one by one
// function * eachline (s) {
//   let p
//   while ((p = s.indexOf('\n')) !== -1) {
//     yield s.substring(0, p)
//     s = s.substring(p + 1)
//   }
//   // return the rest
//   if (s.length > 0) yield s
// }

// let text = ' #comment \n \n hello \nworld\n quit \n unreached \n'
// let lines = eachline(text)
// let trimmed = (l.trim() for (l of lines))
// let nonblank = (l for (l of trimmed) if (l.length > 0 && l[0]!='#'))

// console.log('end')

console.log(window.navigator)